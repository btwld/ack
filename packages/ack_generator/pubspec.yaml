name: ack_generator
description: Code generator for ACK validation schemas
version: 0.1.0
repository: https://github.com/btwld/ack
issue_tracker: https://github.com/btwld/ack/issues
homepage: https://docs.page/btwld/ack

publish_to: none # Internal package, not published

environment:
  sdk: ^3.3.0

dependencies:
  analyzer: ^6.3.0
  build: ^2.4.1
  code_builder: ^4.10.0
  source_gen: ^1.4.0
  dart_style: ^2.3.4
  ack:
    path: ../ack

dev_dependencies:
  build_runner: ^2.4.6
  build_test: ^2.2.0
  lints: ^5.0.0
  test: ^1.25.0
