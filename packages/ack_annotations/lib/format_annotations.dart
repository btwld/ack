/// Format annotations that conflict with Dart core type names.
///
/// Import this library with a prefix to use `@formats.DateTime()` and
/// `@formats.Uri()`.
library;

export 'src/format_constraints.dart';
