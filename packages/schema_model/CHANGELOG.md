## 1.0.0-beta.12-wip

### Added

- Add the `StandardSchema` validation contract, standard results/issues,
  validation options, JSON Schema converter option types, and JSON Schema target
  constants.
