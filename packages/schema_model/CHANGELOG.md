## 1.0.0-beta.12-wip

### Added

- Add the `StandardTyped`, `StandardSchema`, `StandardJsonSchema`, and
  `StandardSchemaWithJsonSchema` contracts, aligned with the official split
  Standard Schema family.
- Add standard results/issues, validation options, JSON Schema converter option
  types, and JSON Schema target constants.
